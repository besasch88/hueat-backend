meta {
  name: List
  type: http
  seq: 2
}

get {
  url: http://127.0.0.1:8001/api/v1/tables?includeClosed=true&target=inside
  body: none
  auth: bearer
}

params:query {
  includeClosed: true
  target: inside
}

auth:bearer {
  token: {{accessToken}}
}

script:post-response {
  bru.setVar("firstTableId", res.body?.items[0]?.id);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
